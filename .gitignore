# Org LaTeX preview image cache (generated by #+STARTUP: latexpreview)
ltximg/

# Byte-compiled Emacs Lisp
*.elc

# early-init.el is tangled locally but shipped by the AppImage build, not the repo
early-init.el

# Emacs autosave / lock files
*~
\#*\#
.\#*
